/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration for PostCSS and Tailwind CSS with .cjs files
  experimental: {
    // Enable experimental features if needed
  },
}

export default nextConfig